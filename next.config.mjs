/** @type {import('next').NextConfig} */
const nextConfig = {
  // Baaki jo settings pehle se thi unhe rehne do, 
  // bas reactCompiler: true ya jo bhi block tha use delete kar do.
};

export default nextConfig;